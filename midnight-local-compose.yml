services:
  proof-server:
    image: "midnightntwrk/proof-server:8.0.3"
    command: ["midnight-proof-server --verbose"]
    ports:
      - "${MIDNIGHT_LOCAL_PROOF_PORT:-6300}:6300"
    environment:
      RUST_BACKTRACE: "full"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6300/version"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 10s

  node:
    image: "midnightntwrk/midnight-node:0.22.1"
    ports:
      - "${MIDNIGHT_LOCAL_NODE_PORT:-3086}:9944"
    environment:
      CFG_PRESET: "dev"
      SIDECHAIN_BLOCK_BENEFICIARY: "04bcf7ad3be7a5c790460be82a713af570f22e0f801f6659ab8e84a52be6969e"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9944/health"]
      interval: 2s
      timeout: 5s
      retries: 20
      start_period: 20s

  indexer:
    image: "midnightntwrk/indexer-standalone:4.0.0"
    ports:
      - "${MIDNIGHT_LOCAL_INDEXER_PORT:-3085}:8088"
    environment:
      RUST_LOG: "indexer=debug,chain_indexer=debug,indexer_api=debug,wallet_indexer=debug,indexer_common=debug,fastrace_opentelemetry=off,info"
      APP__INFRA__NODE__URL: "ws://node:9944"
      APP__APPLICATION__NETWORK_ID: "undeployed"
      APP__INFRA__STORAGE__PASSWORD: "indexer"
      APP__INFRA__PUB_SUB__PASSWORD: "indexer"
      APP__INFRA__LEDGER_STATE_STORAGE__PASSWORD: "indexer"
      APP__INFRA__SECRET: "303132333435363738393031323334353637383930313233343536373839303132"
    healthcheck:
      test: ["CMD-SHELL", "cat /var/run/indexer-standalone/running"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 10s
    depends_on:
      node:
        condition: service_healthy
